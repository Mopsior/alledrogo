import { Form } from "@/components/Form/Form"

export const LoginContent = () => {
    return (
        <div className="container">
            <Form type="login" />
            <Form type="register" />
        </div>
    )
}